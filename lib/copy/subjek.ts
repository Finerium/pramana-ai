/**
 * String konsol simulasi pembukuan (rute /pembukuan). Dimiliki unit h
 * (Phase 1 wave 2). Teks diekstrak verbatim dari
 * design-handoff/subjek/Konsol Pembukuan.dc.html; header dan preset memakai
 * kunci beku dari lib/copy (6.15).
 */
export const SUBJEK_COPY = {} as const;
